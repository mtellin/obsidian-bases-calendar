filters:
  and:
    - file.hasTag("event")
views:
  - type: calendar
    name: Calendar
    order:
      - file.name
      - note.people
    startDate: note.date
    endDate: note.endDate
    colorProperty: note.color
    weekStartDay: monday
    scrollToTime: "08:00:00"
